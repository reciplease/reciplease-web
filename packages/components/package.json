{
  "name": "@reciplease/components",
  "version": "0.1.0",
  "description": "Reuseable UI Components for Reciplease.",
  "main": "./lib/index.js",
  "scripts": {
    "lint:types": "tsc",
    "build": "yarn build:tsc && yarn build:css",
    "build:css": "copyfiles -u 1 ./src/**/*.css ./lib",
    "build:tsc": "tsc --build tsconfig.json"
  }
}
