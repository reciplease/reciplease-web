{
  "name": "@reciplease/components",
  "version": "0.1.0",
  "description": "Reuseable UI Components for Reciplease.",
  "main": "./lib/index.js",
  "scripts": {
    "lint:types": "tsc",
    "build": "tsc --build tsconfig.json"
  }
}
