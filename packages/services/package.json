{
  "name": "@reciplease/services",
  "version": "0.1.0",
  "description": "Reuseable Data Layer for Reciplease.",
  "main": "./lib/index.js",
  "scripts": {
    "lint:types": "tsc",
    "build": "tsc --build tsconfig.json"
  }
}
