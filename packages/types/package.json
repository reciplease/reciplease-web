{
  "name": "@reciplease/types",
  "version": "0.1.0",
  "description": "Typescript types for Reciplease.",
  "main": "./lib/index.js",
  "scripts": {
    "lint:types": "tsc",
    "build": "tsc --build tsconfig.json"
  }
}
